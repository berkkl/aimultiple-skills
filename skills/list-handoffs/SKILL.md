---
name: list-handoffs
description: Show all available handoff documents.
---

List all `.md` files in the `handoffs/` directory. For each file, show the name (filename without extension) and the first line (title). Mark which one is the LATEST by reading `handoffs/LATEST`.

If no `handoffs/` directory exists or it is empty, say "No handoffs found."
